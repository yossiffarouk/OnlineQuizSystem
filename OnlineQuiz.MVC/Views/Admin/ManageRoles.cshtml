<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Roles</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/meyer-reset/2.0/reset.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito:wght@600&display=swap">
    <style>
        html, body {
            height: 100%;
            margin: 0; /* Remove default margin */
        }

        body {
            display: flex;
            flex-direction: column; /* Allow vertical stacking */
            align-items: center; /* Center items horizontally */
            background-color: #f0f0f0;
            font-family: 'Nunito', sans-serif;
        }

        .header {
            background: linear-gradient(135deg, #4e73df, #2e59d9); /* Gradient background */
            color: white; /* Title color */
            padding: 1rem 1rem; /* Reduced padding for a smaller title area */
            border-radius: 15px; /* Increased border radius */
            text-align: center;
                      /* Increased space below title */
            margin: 2rem 0 3rem 0;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3); /* Enhanced shadow for depth */
            transition: transform 0.3s; /* Transition for hover effect */
        }

            .header h1 {
                font-size:2rem; /* Smaller title size */
                letter-spacing: 0.05em; /* Added letter spacing */
                margin: 0;
                font-weight: 700; /* Bold title */
                text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.4); /* Shadow effect for text */
            }

        .btn {
            background: hsl(var(--hue), 98%, 80%);
            border: none;
            border-radius: 10px; /* Rounded buttons */
            cursor: pointer;
            color: black; /* Button text color */
            font: 600 1.2rem/1 "Nunito", sans-serif; /* Medium button text size */
            letter-spacing: 0.05em;
            padding: 1.5em 3em; /* Adjusted padding for medium button size */
            position: relative;
            text-transform: lowercase;
            margin-bottom: 0.25%; /* Small space between buttons vertically */
            transition: background-color 0.3s; /* Added transition for hover effect */
           
        }

            .btn:hover {
                background: hsl(var(--hue), 98%, 70%); /* Darker background on hover */
            }

        .btn--yellow {
            --hue: 46;
        }

        .btn--green {
            --hue: 163;
        }

        .btn--purple {
            --hue: 244;
        }

        .btn--red {
            --hue: 0;
        }

        .btn--blue {
            --hue: 210;
        }

        .btn--light {
            --hue: 60;
        }

        .btn--dark {
            --hue: 90;
        }

        .btn__bg {
            background: hsl(var(--hueBg), 98%, 80%);
            border-radius: 50%;
            display: block;
            height: 0;
            left: 50%;
            margin: -50% 0 0 -50%;
            padding-top: 100%;
            position: absolute;
            top: 50%;
            width: 100%;
            transform: scale(0);
            transform-origin: 50% 50%;
            transition: transform 0.175s cubic-bezier(0.5, 1, 0.89, 1);
            z-index: 1;
        }

        .btn__txt {
            position: relative;
            z-index: 2;
            text-transform: uppercase;
        }

        .btn:hover .btn__bg {
            transform: scale(1.5);
            transition: transform 0.35s cubic-bezier(0.11, 0, 0.5, 0);
        }

        .role-buttons {
            display: grid;
            grid-template-columns: repeat(2, 1fr); /* 2 columns */
            gap: 25%; /* Larger space between columns */
            max-width: 800px; /* Optional: to limit the max width of the grid */
            margin: 0 auto; /* Center the grid */
            padding-right: 125px;
        }

            .role-buttons .btn {
                width: 100%;
                text-align: center;
            }
    </style>
</head>
<body>

    <div class="container">
        <div class="header">
            <h1>Manage Roles</h1>
            <p>Select an action to perform</p>
        </div>

        <div class="role-buttons">
            <a href="/Admin/AddRole" class="btn btn--purple">
                <span class="btn__txt">Add Role</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/DeleteRole" class="btn btn--red">
                <span class="btn__txt">Delete Role</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/RestoreRole" class="btn btn--blue">
                <span class="btn__txt">Restore Role</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/GetUsersInRole" class="btn btn--yellow">
                <span class="btn__txt">View Users in Role</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/AddRoleToUser" class="btn btn--green">
                <span class="btn__txt">Assign Role to User</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/RemoveRoleFromUser" class="btn btn--light">
                <span class="btn__txt">Remove Role from User</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/GetAllRoles" class="btn btn--dark">
                <span class="btn__txt">View All Roles</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
            <a href="/Admin/GetAllRolesIsDeleted" class="btn btn--purple">
                <span class="btn__txt">View Deleted Roles</span>
                <i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i><i class="btn__bg"></i>
            </a>
        </div>
    </div>

</body>
</html>
